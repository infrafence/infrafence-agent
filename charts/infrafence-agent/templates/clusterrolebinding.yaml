apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "infrafence-agent.fullname" . }}
  labels:
    {{- include "infrafence-agent.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "infrafence-agent.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "infrafence-agent.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
