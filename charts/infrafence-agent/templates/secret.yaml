apiVersion: v1
kind: Secret
metadata:
  name: {{ include "infrafence-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "infrafence-agent.labels" . | nindent 4 }}
type: Opaque
data:
  api-key: {{ .Values.apiKey | b64enc | quote }}
