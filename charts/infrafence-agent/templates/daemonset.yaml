apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "infrafence-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "infrafence-agent.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "infrafence-agent.selectorLabels" . | nindent 6 }}
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        {{- include "infrafence-agent.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "infrafence-agent.serviceAccountName" . }}
      hostNetwork: true
      hostPID: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: infrafence-agent
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            privileged: true
          env:
            - name: INFRAFENCE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "infrafence-agent.fullname" . }}
                  key: api-key
            - name: INFRAFENCE_SERVER_URL
              value: {{ .Values.serverUrl | quote }}
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: DAEMONSET_NAME
              value: {{ include "infrafence-agent.fullname" . | quote }}
            {{- if .Values.clusterName }}
            - name: CLUSTER_NAME
              value: {{ .Values.clusterName | quote }}
            {{- end }}
            {{- if not .Values.autoUpdate.enabled }}
            - name: DISABLE_K8S_AUTO_UPDATE
              value: "true"
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: host-log
              mountPath: /var/log
              readOnly: true
            - name: docker-sock
              mountPath: /var/run/docker.sock
              readOnly: true
            - name: containerd-sock
              mountPath: /run/containerd/containerd.sock
              readOnly: true
            - name: agent-config
              mountPath: /etc/infrafence
      volumes:
        - name: host-log
          hostPath:
            path: /var/log
            type: DirectoryOrCreate
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
        - name: containerd-sock
          hostPath:
            path: /run/containerd/containerd.sock
        - name: agent-config
          hostPath:
            path: /etc/infrafence
            type: DirectoryOrCreate
